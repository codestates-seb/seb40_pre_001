import styled from 'styled-components';

const Container = styled.div`
  margin-bottom: 19.5px;
`;

const Header = styled.h2`
  color: hsl(210, 8%, 25%);
  font-size: 1.5rem;
  font-weight: 400;
  line-height: 1.3px;
  margin: 0 0 1em;
`;

const CountSpan = styled.span`
  margin-left: 5px;
  color: hsl(210, 8%, 45%);
  font-size: 11px;
`;

const Ul = styled.ul`
  list-style: none;
`;

export { Container, Header, CountSpan, Ul };
